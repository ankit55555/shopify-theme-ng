{% comment %} <p>Shubham</p> {% endcomment %}

<!-- <div class="controls">
      <label for="driver">Show Driving Element?</label>
      <input type="checkbox" id="driver" />
    </div> -->
    <div class="header">
      <h1>Scroll 👇</h1>
    </div>
    <div class="main_1">
      <div class="section">
        <p>
          <span contenteditable=false>With our open ear buds, your ears will thank you for the perfect mix of safety and sound—because you deserve both.</span>
        </p>
      </div>
    </div>
    <div class="footer" >
      <h2>fin.</h2>
    </div>


 <style>
   /* @font-face {
  font-family: "Geist Sans";
  src: url("https://assets.codepen.io/605876/GeistVF.ttf") format("truetype");
}

body {
  background-color: hsl(0 0% 2%);
  color: hsl(0 0% 100% / 0.1);
  font-family: "Geist Sans", sans-serif;
  font-weight: bold;
  accent-color: red;
  overflow-x: hidden;
  font-weight: 120;
}

:root:has(#driver:checked) main {
	outline: 1rem dashed hsl(0 80% 50% / 0.5);
	background: hsl(0 80% 50% / 0.1);
}
 */
.header {
  height: 100vh;
  color: hsl(0 0% 100%);
  display: grid;
  place-items: center;
  font-size: clamp(2rem, 4vw + 1rem, 12rem);
  background-color: hsl(0 0% 2%);
}

.footer {
  height: 100vh;
  color: hsl(0 0% 100%);
  display: grid;
  place-items: center;
  font-size: clamp(2rem, 4vw + 1rem, 12rem);
  background-color: hsl(0 0% 2%);
}

.header h1 {
  font-weight: 120;
  background: linear-gradient(hsl(0 0% 100%), hsl(0 0% 50%));
  background-clip: text;
  color: transparent;
      font-size: 70px;
/*  text-transform: uppercase;*/
}

.footer h2 {
  font-weight: 120;
  background: linear-gradient(hsl(0 0% 100%), hsl(0 0% 50%));
  background-clip: text;
  color: transparent;
/*  text-transform: uppercase;*/
      font-size: 70px;
}

.controls {
  position: fixed;
  top: 1rem;
  right: 1rem;
  padding: 1rem 2rem;
  border-radius: 100px;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  background: hsl(0 0% 90%);
  color: hsl(0 0% 10%);
}

.section p {
  /* resize: both;
  max-width: 70vw;
  overflow: hidden;
  padding: 10ch;
	margin: 0; */

  resize: both;
    max-width: 100vw;
    overflow: hidden;
    padding: 4ch;
    margin: 0;

  
}

.section p > span {
	outline-color: hsl(10 80% 50%);
	outline-offset: 1ch;
	font-size: clamp(3rem, 4vw + 1rem, 10rem);
  color: hsl(0 0% 100% / 0.1);
  text-decoration: none;
  background-image:
		/* First one is the highlight */
		linear-gradient(90deg, transparent calc(100% - 8ch), hsl(0 0% 100%) calc(100%  - 8ch)),
  	linear-gradient(90deg, hsl(0 0% 50%), hsl(0 0% 50%)),
    linear-gradient(90deg, hsl(0 0% 50% / 0.15), hsl(0 0% 50% / 0.15));
  background-size:
  	200vmax 1lh,
  	200vmax 1lh,
    100% 1lh;
  background-repeat: no-repeat;
  background-position-x: 0;
  background-position-y: 100%;
  background-clip: text;
  color: hsl(0 0% 100%);
}

/*p > span {
  background-clip: unset;
  color: transparent;
  background-position-x: -200vmax, -200vmax, 0;
  animation: fill-up 4s infinite linear;
}

@keyframes fill-up {
  to { background-position-x: 0, 0, 0; }
}*/

.main_1 {
  height: 200vh;
  background-color: hsl(0 0% 2%);
}

.section {
  position: sticky;
  top: 0;
	height: 100vh;
	width: 100vw;
  display: grid;
  place-items: center;
  background-color: hsl(0 0% 2%);
}


@supports (animation-timeline: scroll()) {
  @media (prefers-reduced-motion: no-preference) {
    .main_1 {
      view-timeline-name: --section;
    }
    
   .section p > span {
      background-position-x:
        calc(200vmax * -1),
        calc(200vmax * -1),
        0;
      color: transparent;
      animation-name: fill-up, color-in;
      animation-fill-mode: both;
      animation-timing-function: linear;
    	animation-timeline: --section;
    	animation-range: entry 100% cover 50%, cover 50% exit 0%;
    }

    @keyframes fill-up {
    	to { background-position-x: 0, 0, 0; }
    }

    @keyframes color-in {
    	to { color: hsl(0 0% 100%); }
    }
  }
}
 </style>   